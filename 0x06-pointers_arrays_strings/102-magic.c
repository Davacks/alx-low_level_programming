#include <stdio.h>
/**
 * main - you are not allowed to use a
 * - you are not allowed to modify p
 * - only one statement
 * - you are not allowed to coe anything else than this line of code
 */

int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &n;

p[5] = 98;
/*This will print 98\n */
printf("a[2] = %d\n", a[2]);
return (0);
}
